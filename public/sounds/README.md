# Notification sound

Place your notification sound file here:

- **`notification.mp3`** – played when a new order (online, ambassador, POS, etc.) triggers a browser notification.

If `notification.mp3` is missing or fails to load, the app falls back to a short beep so notifications still make a sound.
